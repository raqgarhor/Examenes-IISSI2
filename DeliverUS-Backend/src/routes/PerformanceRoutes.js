// This is a new file for solution!
// Descomentar: import { hasRole, isLoggedIn } from '../middlewares/AuthMiddleware.js'
// Descomentar:import * as PerformanceMiddleware from '../middlewares/PerformanceMiddleware.js'
// Descomentar:import { handleValidation } from '../middlewares/ValidationHandlingMiddleware.js'

const loadFileRoutes = (app) => {
  // TO-DO
  // Introduce the needed routing
}

export default loadFileRoutes
